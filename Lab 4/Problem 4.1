#include<iostream>
#include<string.h>

using namespace std;

class Student
{
    public:
        string name[6];
        int id[6];
        float cg[6];
};

void store()
{
    string iname;
        int iid;
        float icg;
    cout<<"Enter Name, ID, CGPA:\n";
                cin>>iname;
                Student name=iname;
                cin>>iid;
                id=iid;
                cin>>icg;
                cg=icg
                cout<<endl;
}

void sorting()
        {
            int min_index =0;
            float temp=0;
            int tid=0;
            string tname="";

            cout<<"Enter Name, ID, CGPA:\n";
            for(int i=0; i<6;i++)
            {
                cin>>name[i];
                cin>>id[i];
                cin>>cg[i];
                cout<<endl;
            }

            for(int i=0; i<6;i++){
                cout<<name[i]<<" ";
                cout<<id[i]<<" ";
                cout<<cg[i]<<" "<<endl;
            }
            cout<<endl<<"---------"<<endl;
            cout<<"After sorting: ";

            for(int i=0; i<6; i++)
            {
                min_index = i;

                for(int j=i+1; j<6; j++)
                {
                    if(cg[min_index] > cg[j])
                    {
                        min_index = j;
                    }
                }

                tname = name[min_index];
                name[min_index] = name[i];
                name[i] = tname;

                tid = id[min_index];
                id[min_index] = id[i];
                id[i] = tid;

                temp = cg[min_index];
                cg[min_index] = cg[i];
                cg[i] = temp;
            }

            for(int i=0; i<6;i++){
                cout<<name[i]<<" ";
                cout<<id[i]<<" ";
                cout<<cg[i]<<" "<<endl;
            }

            cout<<endl;
        }

int main()
{
    int n;
    cout<<"How many students: ";
    cin>>n;

    for(int j=0; j<n;j++)
    {
        Student s[j];
    }

    for(int j=0; j<n;j++)
    {
        s[j].store();
    }




    return 0;
}
